# Simple Task Manager (Vanilla JS)

A lightweight task manager built using **pure JavaScript** with support for:
- Classes, Promises, async/await
- Callbacks and mock APIs
- DOM manipulation and event delegation
- LocalStorage persistence


## 📁 Structure

/js
├── app.js
style.css
index.html

## 🚀 How to Run

 Open `index.html` in any browser — no setup required.

 ## 💡 Concepts Implemented
- **Classes** with methods and properties.
- **Promises**, **async/await**, and **callbacks**.
- **try/catch** for error handling.
- **LocalStorage** for persistence.
- **DOM APIs** for dynamic rendering.
- **Event Delegation** for efficient event handling.
- **Template Literals** and modern JS syntax.